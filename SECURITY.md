# Security

Codevia Cursor stores Cursor API keys in VS Code `SecretStorage` and never transmits them to any server. Repositories are treated as potentially hostile input. See `docs/SECURITY.md` for the full security policy.
